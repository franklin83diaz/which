## Which
Which is a command-line tool written in Go that provides functionality similar to the Unix which command, 

note: it does not interpret aliases or shell built-in commands.
Example alias "ll" o shell built-in command "cd"

..._I know, is redundant or superfluous_


## Features
Finds the location of executable commands on the system.

## Installation
To install Which, first ensure you have Go installed on your system. Then, you can install Which with the following command:

```
go build -o which main.go
```

### Usage
To use Which, simply run the which command followed by the name of the command you're looking for. For example:
```
which ls
```
This will print the path to the ls command.



## Contributing

Contributions to MyWhich are welcome. Please open an issue to discuss the changes you would like to make before submitting a pull request.

## License
MyWhich is licensed under the MIT license. See the LICENSE file for more details.